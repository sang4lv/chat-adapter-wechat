---
"chat-adapter-wechat": minor
---

Add custom storage support for ACP adapter via `accountStorage` and `stateStorage` callbacks, allowing persistence to databases or other backends instead of the filesystem
